# Nixpacks config for the backend service on Railway.
# Monorepo layout: backend/ holds the Python app, frontend/ is a separate Railway service.
#
# Key lessons:
#   - Nix's python312 is immutable — use a venv for all pip installs.
#   - pandas + numpy require libstdc++.so.6 which conflicts with Railway's glibc version.
#     Solution: remove pandas and numpy entirely and use pure Python math instead.
#     No pandas = no numpy = no libstdc++ = no glibc version mismatch.

[phases.setup]
nixPkgs = ["python312", "nodejs_20"]

[phases.install]
cmds = [
  "python3.12 -m venv /opt/venv",
  "/opt/venv/bin/pip install --upgrade pip",
  "/opt/venv/bin/pip install -r backend/requirements.txt",
  "cd frontend && npm install && npm run build"
]

[start]
cmd = "cd backend && /opt/venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
